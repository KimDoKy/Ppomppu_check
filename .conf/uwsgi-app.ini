[uwsgi]
chdir = /srv/app/ppomppu
module = conf.wsgi.prod

uid = www-data
gid = www-data

socket = /tmp/ppomppu.sock
chmod-socket = 666
chown-socket = www-data:www-data

enable-threads = true
vacuum = true
pidfile = /tmp/ppomppu.pid
lazy-apps = true
log-reopen = true
